// Place all the styles related to the User controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/
body {
     font-size: 1.5em;     
}
form {
     width: 80%;
     div {
     	 width: 80%;
     }
     .form_label {
     		 width: 30%;
		 text-align: right;
		 display: inline-block;
     }
     .field_with_errors {
                 display:inline;     	
     }

     input {
     	   width: 50%;
	   font-size: 1.5em;	   
     }
}

.submit_button {
	background: #248F24;
	/*background: -webkit-linear-gradient(top, #33CC33, #70DB70);
	background: -o-linear-gradient(bottom, #33CC33, #70DB70);
	background: -moz-linear-gradient(bottom, #33CC33, #70DB70);*/
	border-radius: 0.4em;
	width: 20%;
	color: white;
	margin-left: 30%;
}

.submit_button:hover {
	       background: linear-gradient(to bottom, #70DB70, #33CC33);
}